# JWT 로그아웃과 무효화 처리

JWT는 한번 발급되면 서버가 강제로 무효화하기 어렵다는 한계가 있다. 세션 방식은 서버가 세션을 즉시 삭제하면 로그아웃 처리가 끝나지만, JWT는 서명 검증만으로 인증하기 때문에 서버가 특정 토큰만 골라 무효화할 방법이 없다.

이를 보완하기 위해 Access Token의 만료 시간을 짧게 설정하고, Refresh Token은 서버가 별도로 관리하며, 로그아웃이나 탈취 시에는 무효화해야 할 토큰을 블랙리스트에 등록해 대응한다.
